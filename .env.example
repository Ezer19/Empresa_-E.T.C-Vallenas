APP_NAME="ETC Vallenas"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost
APP_TIMEZONE=America/Cusco

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# MongoDB Database Configuration
DB_CONNECTION=mongodb
DB_HOST=127.0.0.1
DB_PORT=27017
DB_DATABASE=etc_vallenas
DB_USERNAME=
DB_PASSWORD=
DB_AUTHENTICATION_DATABASE=admin

# Cache & Session Configuration
BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_DOMAIN=
SESSION_SECURE_COOKIE=false

# Memory Cache
MEMCACHED_HOST=127.0.0.1

# Redis Configuration
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Mail Configuration
MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="noreply@etcvallenas.com"
MAIL_FROM_NAME="${APP_NAME}"

# File Storage Configuration
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Real-time Communication
PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# ETC Vallenas Specific Configuration
COMPANY_NAME="ETC Vallenas"
COMPANY_EMAIL="info@etcvallenas.com"
COMPANY_PHONE="+51-xxx-xxx-xxx"
COMPANY_ADDRESS="Lima, Perú"
COMPANY_WEBSITE="https://www.etcvallenas.com"

# Business Configuration
RENTAL_TAX_RATE=18.00
CURRENCY=PEN
CURRENCY_SYMBOL="S/."
DEFAULT_LANGUAGE=es
DEFAULT_TIMEZONE=America/Lima

# Maquinaria Configuration
MAX_IMAGE_SIZE=5120
ALLOWED_IMAGE_TYPES="jpg,jpeg,png,webp"
IMAGES_PER_MACHINE=10
MACHINE_AVAILABILITY_BUFFER_HOURS=24

# PDF Generation (DomPDF)
DOMPDF_DEFAULT_FONT=DejaVu Sans
DOMPDF_DEFAULT_PAPER_SIZE=A4
DOMPDF_DEFAULT_ORIENTATION=portrait
DOMPDF_ENABLE_REMOTE=true

# Image Processing (Intervention)
IMAGE_DRIVER=gd
IMAGE_QUALITY=90
THUMBNAIL_WIDTH=300
THUMBNAIL_HEIGHT=200

# Security Configuration
PASSWORD_MIN_LENGTH=8
SESSION_REMEMBER_DURATION=2592000
MAX_LOGIN_ATTEMPTS=5
LOGIN_LOCKOUT_DURATION=900

# API Configuration
API_RATE_LIMIT=60
API_VERSION=v1
API_PREFIX=api

# File Upload Configuration
MAX_UPLOAD_SIZE=10240
UPLOAD_PATH=uploads
DOCUMENT_UPLOAD_PATH=documents
IMAGE_UPLOAD_PATH=images

# Backup Configuration
BACKUP_DISK=local
BACKUP_SCHEDULE=daily
BACKUP_RETENTION_DAYS=30

# Notification Configuration
NOTIFICATION_CHANNELS=mail,database
SMS_PROVIDER=
SMS_API_KEY=
SMS_FROM_NUMBER=

# Analytics Configuration
GOOGLE_ANALYTICS_ID=
GOOGLE_MAPS_API_KEY=

# Social Media Configuration
FACEBOOK_URL=
INSTAGRAM_URL=
LINKEDIN_URL=
YOUTUBE_URL=

# Maintenance Mode
MAINTENANCE_MODE=false
MAINTENANCE_MESSAGE="Sitio en mantenimiento. Vuelva pronto."

# Development Tools
TELESCOPE_ENABLED=false
DEBUGBAR_ENABLED=false
CLOCKWORK_ENABLED=false

# Production Settings
FORCE_HTTPS=false
HSTS_ENABLED=false
SECURE_COOKIES=false
